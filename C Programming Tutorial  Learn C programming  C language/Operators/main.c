#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*Multiplicative and additive operators
    + (addition),
    - (subtraction),
    * (multiplication),
    / (division),
    % (modulus)
    */

    int a=9, b=2;
    int modulus;
    modulus = a % b;
    printf("Rest of the division: %d", modulus);
    return 0;
}
